module.exports = {
  host: 'donutsmp.net',
  port: 25565,
  username: 'YOUR_BOT_ALT_USERNAME_HERE',   // ← CHANGE THIS to your alt account username
  version: '1.21',                          // Works on current Donut SMP
  auth: 'microsoft',                        // Use 'offline' only if server allows cracked accounts

  viewerPort: process.env.PORT || 3000
};
